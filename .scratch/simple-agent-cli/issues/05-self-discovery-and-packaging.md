# 05: Self-discovery and packaging

**What to build:** The CLI is installable as `simple-agent` (built executable with a shebang), documents itself via `--help` and `--version`, and ships a README plus a committed `.env.example` template.

**Blocked by:** 01 (core conversation).

**Status:** ready-for-agent

- [ ] `--help` lists usage, options, and environment variables
- [ ] `--version` prints the installed version
- [ ] The built binary is executable and runs as `simple-agent` when installed
- [ ] README documents setup and usage; `.env.example` is committed and `.env` is ignored
